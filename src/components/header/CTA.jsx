import React from 'react';
//import CV from '../../assets/Karan_resume.pdf';

const CTA = () => {
  return (
    <div className="cta">
      <a href="#" download className="btn">
        Download CV (currently There is not updated CV)
      </a>
      <a href="#contact" className="btn btn-primary">
        Let's talk
      </a>
    </div>
  );
};

export default CTA;
